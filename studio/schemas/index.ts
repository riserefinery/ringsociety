import { callout, definitionList, pageSeo, responsiveImage } from './common'
import { blogLanding, category, contactPage, legalPage, post, publicationSettings, siteSettings } from './documents'

export const schemaTypes = [
  pageSeo,
  responsiveImage,
  callout,
  definitionList,
  siteSettings,
  publicationSettings,
  category,
  post,
  blogLanding,
  contactPage,
  legalPage,
]
